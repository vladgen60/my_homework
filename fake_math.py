    # Создание модуля fake_math.

def divide(first, second):  # Создаем функцию.
    if second == 0: # Условие при котором
        return 'Ошибка' # возвращается ошибка.
    else:
        return first / second   # Возвращаем результат деления.
